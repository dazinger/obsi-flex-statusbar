# obsi-flex-statusbar
The primary purpose of this CSS script is to prevent the status bar from floating over your notes, especially when there are many statuses from different plugins.  I adjusted the nav and sidebar elements to use flex containers and allow customization of the number of columns per row as needed.
